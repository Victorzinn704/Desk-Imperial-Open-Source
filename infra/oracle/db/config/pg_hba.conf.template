local   all             postgres                                peer
local   all             all                                     scram-sha-256
host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             ${DB_DOCKER_SUBNET}     scram-sha-256
host    all             all             ${PROD_WG_IP}/32        scram-sha-256
host    all             all             ${OPS_WG_IP}/32         scram-sha-256
